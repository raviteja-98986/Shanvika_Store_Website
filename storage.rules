rules_version = '2';

// Deploy: enable Storage in Firebase Console first, then npm run deploy:storage
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
